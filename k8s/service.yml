apiVersion: v1
kind: Service
metadata:
  name: brain-tasks-app-service
spec:
  type: LoadBalancer
  selector:
    app: brain-tasks-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
